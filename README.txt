仅供个人学习用，不得作其它用途，尤其不得商用。禁止传播。

总共1000张图片（jpg文件），训练集（train）800张，测试集（test）200张。
label文件夹中的.txt文件中表示为每张图片中每本图书的坐标，每一个行第一个数据是0，表示类别（只有这一个类别）。后面8个数据依次为每本书的4个顶点的横坐标、纵坐标（已归一化）。
注意有些图片最左边和最右边可能有少量书本因为不完整导致顶点数量不是4，可能是3或5。
注意图片的尺寸并不一致。